/**
 * App-wide constants for display and API configuration.
 * Centralizing these improves consistency and makes future changes (e.g. locale/currency) easier.
 */
export const DEFAULT_CURRENCY_CODE = 'EUR' as const;
